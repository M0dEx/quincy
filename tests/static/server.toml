name = "tun0"
certificate_file = "tests/static/server_cert.pem"
certificate_key_file = "tests/static/server_key.pem"
tunnel_network = "10.0.0.1/24"
bind_address = "::"
bind_port = 55155

[authentication]
users_file = "tests/static/users"

[connection]
mtu = 1400

[log]
level = "info"
